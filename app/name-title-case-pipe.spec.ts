import { NameTitleCasePipe } from './name-title-case.pipe';

describe('NameTitleCasePipe', () => {
  it('create an instance', () => {
    const pipe = new NameTitleCasePipe();
    expect(pipe).toBeTruthy();
  });
});
